# Ruff configuration. Default rule set (Pyflakes + pycodestyle E4/E7/E9).
#
# This package is entirely hand-written (no generated code), so nothing is
# excluded from linting.
target-version = "py314"
